#include "math.h"


int main(int)
{
    float a = 3, b = 8;
    float result = 0;
    result = add(a, b);
    result = div(a, b);
    result = mod(a, b);
    result = multi(a, b);
    result = sub(a, b);
    return 0;
}